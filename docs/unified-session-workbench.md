# 统一只读会话工作台

入口是侧栏「全局 → 全部会话」（`/all-sessions`）。支持设置中的 Codex、Claude、Qoder CLI、WorkBuddy、Grok Build CLI、Pi、DSH、Hermes Agent、ZCode 与 OpenCode 十个来源；打开页面不会自动扫描，点击来源卡片可单独读取，顶部刷新可同时读取已配置来源。

## 当前行为

- 每个来源独立显示路径、读取状态、最近成功时间和失败原因。一个来源失败不会清空另一个来源，也不会清除该来源上次成功的结果；旧结果带时间提示。
- 列表按更新时间降序，每页 50 条。支持 Provider、完整项目路径、归档状态筛选，以及标题、首条消息、ID、路径搜索。该搜索不是全文索引；同名项目不按目录 basename 合并。
- 点击标题打开现有对话预览，保持筛选和列表位置。只读模式隐藏编辑、删除、分叉和编辑历史入口；可复制 ID 与路径；只有已有续聊能力的来源显示 resume 命令，不自动启动原生 Agent。
- UI 身份复用 Provider 与实际会话路径组合，避免跨 Provider 同 ID、Claude 同 ID 多个文件发生串选。
- 筛选保存在 URL，并在当前应用会话内记住最近一次视图。切换其他页面后从侧栏返回，恢复来源结果、筛选、页码和滚动位置，不自动重新扫描；带筛选参数的显式 URL 优先于保存的筛选。
- 缓存只保留每个 Provider 当前来源的成功结果（包括空结果、部分完成报告和扫描时间），不落盘。刷新失败或取消不替换缓存；返回时显示上次扫描时间，需手动刷新获取新内容。重新加载页面、关闭应用或打开新窗口后需重新读取。
- 来源目录改变时丢弃受影响的缓存并使旧请求失效；Claude 的来源注记还依赖 Codex 目录，后者改变也会使 Claude 缓存失效。来源组合改变重置保存的列表位置，不复用已离开的来源历史。缓存不代表备份，也不证明源文件仍然存在。
- 来源读取是独立后台任务。页面显示发现文件、读取索引、读取文件等实际阶段，以及已发现、已处理和失败计数；发现与读取交替进行，不把当前发现数量当作最终总量，也不显示估算百分比。
- 「停止扫描」向后端发送取消请求，收到终态后才显示「已停止」。取消不会用未完成结果覆盖上次列表，可再次读取。离开页面会请求取消；启动响应较晚返回时仍会补发取消。断网时取消无法保证送达，重新进入并读取同一来源可接回仍在运行的任务。
- 状态查询失败显示「状态待确认」，保留任务 ID，提供重试查询和停止；不会直接当作扫描已结束或新建重复任务。
- 坏 JSON、缺失/不可读文件和不可遍历目录记录路径与原因，其他文件继续读取；结束时标为「部分完成」。失败详情最多保留 200 项，总数完整计入统计；目录失败也属于失败项。源目录或必需索引整体不可用时仍整体失败并保留旧列表。

## 实现边界

扫描任务复用现有 Codex 索引摘要、Claude 解析、归档解析和来源注记，旧 `list_sessions` 行为保留。没有迁移数据库、配置路径或修改原生会话的写入逻辑；DSH 的 Zstd 解压新增 Rust zstd 依赖。读取成功只表示本次扫描完成，不表示备份健康、完整索引或原生续聊可用。

本功能仍不包含 Registry/FTS、Doctor 诊断或后台自动刷新。任务在内存中保留，最多 10 个运行任务、16 个任务记录，仅淘汰已结束记录；同一 Provider 与来源的运行任务会被复用。运行中状态查询不复制完整会话结果，终态才返回结果。

新扫描入口逐文件先检查 JSONL 格式，再复用原解析器，可能发生两次读取；含坏行的文件整项报告失败，旧列表的宽松解析行为不变。取消在目录项、文件和行处理边界检查，不能中断操作系统正在阻塞的单次读取。JSONL 来源按文件计数，SQLite 来源按会话计数；这些计数不代表数据库行数或读取字节数；大规模耗时与内存仍需独立基准验证。

下一步性能建议：合并 JSONL 验证与摘要解析为单次流式读取，并用固定数据集测量扫描耗时、取消延迟与峰值内存。

## 正文搜索与命中定位

- 列表筛选仍只匹配标题、首条消息、ID 和路径；「搜索正文」由用户手动启动，针对筛选后的全部已读取会话，不限于当前页。
- 复用现有 content_search 单活动任务管理，在一个任务内处理上述十个来源的显式会话集合。空集合不发现默认来源；最终文件来自对应来源列表，并在读取前检查路径边界。
- 搜索用户与助手消息，不包括全部工具输出。最多返回 100 个会话，每会话展示 3 处命中；结果达到上限时提示截断。
- 命中携带来源、消息行号和预览偏移，复用只读 PreviewDialog 定位。关闭预览返回原搜索结果，关闭搜索返回原列表页。
- 同一应用会话内保留查询和已结束结果；来源、项目、归档或列表筛选范围变化时失效，不持久化到磁盘。关闭或离页取消任务，迟到启动响应补发取消。
- 工作台搜索按来源协议检查 JSONL、压缩日志或数据库记录，失败会话不发布部分命中；文件消失、来源失败或读取失败会显示报告，最多保留 100 项详情。旧单来源搜索保留原解析行为。
- 来源发现阶段尚无最终字节总量；进入正文读取后展示已读字节与总字节。阻塞中的单次系统 IO 仍不能立即中断。

## Qoder CLI 只读来源

设置页新增 Qoder CLI 配置目录，默认遵循 `QODER_CONFIG_DIR`，否则使用 `~/.qoder`。工作台手动读取 `projects/<project>/<session>.jsonl`，复用既有可取消扫描、失败报告和搜索任务。Qoder 入口为「全局 → 全部会话」，不启用原生写入、删除、恢复、续聊或格式转换。

格式证据来自 [Qoder CLI 官方会话文档](https://docs.qoder.com/zh/cli/sessions)；回归复用 kage 的 MIT 夹具（CLI 0.1.29），来源与锁定 commit 见 THIRD_PARTY_NOTICES.md。不把 Qoder IDE、QoderWork、执行日志或子代理目录当作主会话。官方新 `state.json` 没有稳定公开 schema，本轮不依赖它推断会话正文。

## 长会话预览

预览工具栏可切换「最早在前／最新在前」，也可直接跳到最早或最新。跳到末尾和搜索命中时按目标位置读取 200 条事件窗口，保留原始事件索引用于定位。消息区与时间线复用 TanStack 虚拟列表，只挂载可视区域附近节点；展开很长的过程分组时分段显示。

此优化降低浏览器渲染与传输开销，不意味着 JSONL 底层 IO 已有随机访问索引。显式加载全部仍会占用与会话大小相关的内存；首次提问时间线读取仍需遍历来源文件。原生文件不被改写。

## 本轮验证记录（2026-09-20）

- 前端 86 项测试、生产构建、版本一致性检查通过；Rust Qoder 专项 5 项及无桌面默认特性的完整库测试 534 项通过。增加坏文件隔离回归：未选中的不可读文件不影响搜索，选中的坏文件单独报告，健康会话继续返回命中。
- 实际构建页面使用 20,000 条事件和 10,000 条提问的接口夹具验收：远处命中只请求偏移 14,900 的 200 条窗口，最新快捷入口请求偏移 19,800 的 200 条窗口；顺序切换可返回首条消息。修复过滤结果不足一屏时意外自动连读后续全部页面的问题。
- 消息和时间线虚拟渲染生效，桌面时间线实际挂载约 59 个刻度节点；Qoder 操作菜单没有写入、删除或续聊入口。1280×720 与 390×844 截图检查通过，窄窗口无横向溢出，浏览器控制台无错误。此部分后端为固定夹具，不作为原生扫描或安装包验收证据。
- 无桌面默认特性的 CLI 构建通过；真实 Rust WebUI 使用独立临时设置及 Qoder 文件，验证 2 个主文件中正常会话可用、损坏 JSON 单项失败、子 Agent 文件不计入扫描；正文搜索返回用户与助手命中，并可打开相应正文。读取前后源文件清单及 SHA-256 一致。安全矩阵 12/12、`cargo fmt --all -- --check` 通过；未进行原生 Tauri 窗口或目标平台安装验收。

## 历史验证记录

2026-09-14 正文搜索追加验证：前端 82 项、Rust content_search 12 项、安全矩阵 12/12 通过，前端构建通过。内嵌 Chromium 使用固定接口夹具验证 123 条跨来源范围不受第 2 页限制、命中摘要及预览消息定位、关闭预览返回结果、导航返回保留查询、运行中取消、来源筛选变更清空旧结果与失败/截断提示。1280×720 和 390×844 页面检查通过，无横向溢出或控制台错误。Rust 临时文件测试覆盖空范围不发现默认来源、坏 JSON、范围外文件排除、缺失文件报告及旧搜索回归。浏览器后端为夹具，未完成原生 Tauri 窗口和目标平台安装运行验收；跨平台编译与完整测试由 CI 单独验证。

2026-09-13 返回体验追加验证：前端 80 项测试通过，前端构建通过。实际构建页面在内嵌浏览器中使用固定接口夹具（121 条 Codex 结果），验证第 2 页、筛选词和 1796 px 滚动位置在导航返回后恢复，且没有追加扫描请求；刷新失败、取消、离页取消、延迟启动补取消和来源变更隔离均通过。1280×720 与 390×844 视觉检查无横向溢出，控制台无错误。此轮验证覆盖真实渲染交互，接口与扫描为夹具，不代表原生 Tauri 窗口或新增后端扫描验收；后端未修改。

可取消扫描追加验收：前端 77 项、Rust lib 525 项通过，前端及无桌面默认特性的 CLI 构建通过。真实 WebUI 使用 21.6 MB 临时 JSONL，验证实际进度、读取中取消、取消后旧列表不变、重新扫描、坏 JSON 单项失败，以及注入状态查询 500 后复用同一任务。延迟启动响应验证启动尚未返回时停止、导航离开后补发取消和旧结果隔离。源夹具目录清单与哈希不变，页面无脚本异常，1280×820 与 390×844 截图检查通过。仍未进行 Tauri 原生窗口或 macOS/Linux 验收。

工作台第一步的历史验证记录：

- `npm run test:frontend`：76 项通过。`npm run build` 与 `git diff --check` 通过；构建保留既有 vendor-charts 大分块提示。
- 真实 WebUI 接口读取临时 Codex 会话及两份同 ID、不同文件的 Claude 会话，三个预览分别命中正确正文；没有编辑、删除、分叉或恢复请求，原生源夹具目录清单与文件哈希不变。
- 验证首次打开不自动读取、来源/项目/文本筛选、预览返回保留筛选；注入单来源 500 响应验证旧结果保留与重试。控制台只有该预期 500，无页面脚本异常或框架错误遮罩。
- 1280×820、390×844 截图检查通过；另用 121 条合成列表响应验证 50/50/21 分页与筛选后页码重置。这不是大规模原生扫描性能验收。
- 本轮未修改 Rust；未重跑 Rust 全量，也未进行 Tauri 原生窗口或 macOS/Linux 验收。Browser plugin not available，使用已有 Playwright 与本机 Edge 完成上述 WebUI 检查。

## WorkBuddy、官方 Grok CLI 与 Pi（alpha.9）

在设置中配置来源，再从「全局 → 全部会话」读取。该版本的六个来源可同时刷新，新接入来源均为只读。

| 来源 | 默认目录 | 读取范围与边界 |
| --- | --- | --- |
| 腾讯 WorkBuddy | ~/.workbuddy | projects/<project>/<session>.jsonl；user_query 中用户正文与 assistant output_text；不读取数据库自定义标题、图片或工具输出为正文 |
| 官方 xAI Grok Build CLI | GROK_HOME 或 ~/.grok | sessions/<project>/<id>/summary.json + updates.jsonl；流式片段聚合，排除 rewind 旧分支及隐藏子 Agent |
| Pi | PI_CODING_AGENT_DIR 或 ~/.pi/agent | sessions 下 JSONL；复用 provider-pi v1/v2/v3 解析与当前分支路径 |

WorkBuddy 指腾讯产品，与旧计划中的 work-buddy.ai 元数据叠加工具不同；未修改 provider-workbuddy 的原有合同。Grok 指 xai-org/grok-build，不包含 Grok 网页/App 或第三方同名 CLI。

来源依据：workbuddy-exporter@52189e6ae48cb1acb90afaa92b729d99d1f1b1bb 的提取规则；Grok 官方 grok-build@4247f661689354b831191f11eeeac8424993fe3d 的 Summary、updates 和 rewind；Pi 复用本仓库已有 SDK 解析器。对应许可证和移植范围见 THIRD_PARTY_NOTICES.md。

搜索与预览使用同一事件投影，保留原始行号与投影偏移。Grok/Pi 单文件仍完整解析后分页，取消检查覆盖读取与投影循环（Pi SDK 的单次文件读取完成后检查），未引入 FTS 或跨语言运行环境。坏文件/半写入尾行报错，不以完整成功结果交付；所有测试使用合成临时会话。

2026-09-23 本地验证：`npm run test:frontend` 89/89，`npm run build`，`cargo test -p cc-session-manager --no-default-features --lib -j2` 545/545，无桌面默认特性的 CLI 构建，安全矩阵 12/12，格式检查及 alpha.9 发布元数据检查全部通过。新增测试覆盖三来源扫描深度/坏文件隔离/取消、Grok 连续 rewind 与流式聚合、Pi 当前分支/源文件变化，以及跨来源搜索与预览位置一致性。

真实 Rust WebUI 使用独立临时设置和合成原生文件，验证三来源读取、WorkBuddy 单项损坏报告、正文搜索、Grok 跨片段短语命中、Pi 当前分支命中、打开预览再返回搜索、只读操作菜单和 WorkBuddy 上下文排除。1280×820 与 390×844 视觉检查无横向溢出或页面错误，读取前后所有源夹具 SHA-256 不变。Browser plugin not available，使用已有 Playwright + Edge。完整 Tauri/平台编译由 CI 核验；此处不代表原生安装运行、签名或公证验收。前端保留既有 vendor-charts 大分块提示。


## DSH、Hermes、ZCode 与 OpenCode（alpha.10）

本轮新增三个只读适配器，并把已有 OpenCode 解析器接入统一工作台。十个来源可同时刷新；来源筛选、缓存隔离、可取消扫描、失败报告、正文搜索和命中预览沿用工作台交互。

| 来源 | 配置目录与读取位置 | 支持范围 |
| --- | --- | --- |
| DSH / DeepSeek Harness | `DSH_HOME` 或 `~/.dsh`；读取 `sessions/<project>/<session>/session.vN.jsonl[.zstd]` | v3/v4 canonical 文件，原始 JSONL 和连续 Zstd 帧；投影用户消息、助手消息及工具事件 |
| Hermes Agent | `HERMES_HOME` 或 `~/.hermes`；读取 `state.db` | sessions/messages；检测可选字段，按 timestamp/id 排序 |
| ZCode | `~/.zcode`；读取 `cli/db/db.sqlite` | session/message/part；优先按原生 sequence 排序，兼容缺少 sequence 的排序回退 |
| OpenCode | 复用设置中的 OpenCode 数据目录；读取 `opencode.db` | 复用现有解析与正文搜索，补齐工作台入口和只读预览 |

DSH 同目录只读取最高 canonical 格式代次；不支持该代次时报告失败，不回退旧文件。v0–v2、未知必要事件、序号不连续、截断日志、损坏压缩流，以及同代次有多个物理文件均拒绝读取。单会话解压正文上限为 128 MiB。预览是历史事件的只读投影，不承诺复现原生运行上下文、分支或 surfaceOp 的当前状态；诊断 attempt、注入上下文和 compaction 不作为用户正文搜索，附件只显示占位。此范围不是 DSH 全版本兼容承诺。

Hermes/ZCode 以只读连接与查询事务读取 SQLite，OpenCode 复用现有只读连接；工作台入口检查数据库及存在的 WAL/SHM 路径，不创建、迁移或修复原生数据库。失败详情标记文件或会话；数据库整体不可读时保留上次成功结果。配置目录变化会使对应列表、搜索范围及旧请求失效。

新增三个来源不提供编辑、删除、恢复或原生续聊入口；工作台中的 OpenCode 隐藏写入操作。OpenCode 原有独立页面不在此次能力变更范围内。预览虽按窗口传输与渲染，当前解析仍可能完整读取一个会话，不代表已实现数据库分页查询、随机访问索引或大规模性能验收。

复用来源已锁定并登记在 [第三方声明](../THIRD_PARTY_NOTICES.md)：agent-sessions 的 DSH/Hermes 规则、DeepSeek 官方 v4 事件目录与 canonical 文件规则、ZCode 官方路径和排序规则。只移植小模块与合成测试，不引入上游应用或跨语言运行环境。

2026-09-24 本地验证：前端 93/93、Rust lib 548/548、安全矩阵 12/12 及前端构建通过。新增 Rust 联动测试覆盖四来源扫描、正文搜索与同一投影偏移的命中预览，并检查 SQLite 源文件内容不变、ZCode 原生顺序、取消以及 DSH 连续压缩帧、坏序号、截断和未知版本拒绝。最终 DSH 工具事件补充回归 3/3、格式与版本元数据检查通过。Rust WebUI + Edge 完成四来源扫描、正文搜索、命中预览、返回条件保留、来源筛选与设置保存验证；1280×820、390×844 无横向溢出或应用错误，测试源文件 SHA-256 不变。CI 制品以对应标签工作流结果为准；此记录不表示原生 Tauri 窗口或安装包验收通过。
